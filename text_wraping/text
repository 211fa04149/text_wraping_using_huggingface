1. We have to save the token. 
    - Open the Hugging Face account; you must be logged in to that profile. 
       in profile Scroll down and have their access token; in that write mode, create a token.

      - First, we should run this in cmd using the command 
         "setx HF_API_TOKEN token_id."   
        After opening the Visual Studio platform in that environment, update the token. We have to run the code; it can change the token and run the file.

2. We have to download the 

   -  pip install python-dotenv
   -  pip install requests python-dotenv
   -  pip install reportlab python-dotenv requests
   -  pip install python-docx

3. Run the file test_env.py to see if it saved the API or not.
   -  it is not mandatory : Run the app.py—in this, have a normal question and just prompt a solution. 

4. Run the index.py—in this have the prompt solution, MCQ generation, and assignment questions
     (easy, medium, and hard). 
     PDF and (Word should be shown in the output folder). We can copy the file path and run it in Chrome or Microsoft Edge for Word document.







    




   




